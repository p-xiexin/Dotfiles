- defaults:
    link:
        create: true
        relink: true

- clean: ['~/.config/nvim']

- link:
    ~/.config/nvim: config/nvim

# - create:
#  - ~/downloads
#  - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodules]
